# 路由分组（sprint-1）

sprint-0 把全部路由平铺在 `app/routes.ts`。sprint-1 一次性新增大量页面
（设置壳 / 团队成员 / 邀请 / 403 / 错误页 / 工作台…），若继续平铺会让并行实现
在同一文件互相踩踏，故按功能域拆成分组数组，由 `app/routes.ts` 汇总。

每个分组文件导出一个 `RouteConfigEntry[]`；新增页面只改自己那一组。
分组归属见各文件头部注释（标注对应文档与附录 C 表面号）。
