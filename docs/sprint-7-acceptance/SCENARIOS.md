# Sprint-7 验收视频契约（六幕）

> 录制器：`scripts/acceptance_video_s7.mjs`（`node scripts/acceptance_video_s7.mjs`，
> 单幕 `ONLY=<关键字>`）。视频永不在库（*.webm 全局 gitignore，沿用 sprint-3~6 口径）。
> 前置：API 8000（全量 env）+ web dev 3001 + seed（`python3 scripts/seed_acceptance_s7.py`）。

| 幕 | 内容 | 验收点（sprint-overview §6 / §10.1） |
| --- | --- | --- |
| 01 | 工作流画布全貌 | 画布打开已发布工作流：三态节点（初始圆点/锁定🔒徽标）+ 命名边 + 状态徽标 vN；点边开侧栏（WF-001 §3.1/§3.2） |
| 02 | 守卫拦截→补齐→流转 | 任务抽屉受控流转按钮（边名）→ 点击「提交评审」→ 守卫对话框（缺负责人）→ 下拉补齐 → 状态迁移（WF-004 §3.1 / BR-05 单请求闭环） |
| 03 | 审批五场景核心链 | 发起「发布上线」→ 202 挂起提示 → 切李四登录 → 审批中心待办 Tab → 详情抽屉时间线 → 通过 → 回张三看任务已迁移（WF-002 §2.2 / 终审回填） |
| 04 | 自动化规则页 + Dry Run | 规则列表（启停开关）→ Dry Run 弹层搜任务选中 → 结果 JSON → 运行日志 Tab（C.146，WF-003 §2.4 / BR-11 零写；补口轮起走 UI 路径） |
| 05 | 工时提交→驳回→通过→台账 | 李四提交本周 → 张三队列驳回（附意见）→ 李四重交 → 张三通过 → 台账矩阵行（负载°/冻结🔒）（TASK-013 §3.1~3.3） |
| 06 | 模板库 + 审计页 | 预设四套卡片 → 两步下发向导（预演 BR-05 映射）→ 审计页链完整性徽标 + 事件列表 + 导出（C.145/C.148，WF-005 §4.4 / WF-006 §2.2；补口轮起走 UI 路径） |

全部六幕均为真实页面操作（演示账号 zhangsan / 李四双身份）。04/06 原为
API JSON 回放幕，2026-09-09 入口补口轮（C.144~C.149）交付后切换为 UI 路径。
