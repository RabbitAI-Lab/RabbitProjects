# 企业级统一视图模板

| 文档编号 | BOARD-007 | 所属迭代 | P4（v2 纳入） | 模块 | M5-BOARD |
| --- | --- | --- | --- | --- | --- |
| 文档状态 | 已实现稿（R16，自含精简） | 前置 | BOARD-003 保存视图 | 日期 | 2026-09-12 |

> **范围**：工作空间级「官方视图模板」——管理员发布筛选/列配置模板，
> 成员一键应用到自己项目；模板版本化（发布即快照）。

## BR
- BR-01 模板 = {name, filter_json, columns_json, version}；WS_ADMIN 发布
- BR-02 应用 = 拷贝快照到目标项目视图（不建引用——后续模板演进不影响已应用）

## 端点
GET/POST `/api/v1/workspaces/{slug}/view-templates/`；POST `.../{id}/apply/?project_id=`

## 用例
- UT-01 发布/列表/版本递增
- UT-02 应用落目标项目视图（快照拷贝语义）
